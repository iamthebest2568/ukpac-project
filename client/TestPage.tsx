const TestPage = () => {
  return (
    <div style={{
      backgroundColor: "red",
      height: "100vh",
      display: "flex",
      alignItems: "center",
      justifyContent: "center",
      color: "white",
      fontSize: "24px"
    }}>
      TEST PAGE WORKING - REACT IS MOUNTING
    </div>
  );
};

export default TestPage;
